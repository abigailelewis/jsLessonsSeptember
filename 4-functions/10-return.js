//returns

function fullName(firstName, lastName){
    return firstName + ' ' + lastName;
}

var fullName = fullName("abby", "lewis");
console.log(fullName);

function addNums(firstNum, secondNum){
    return firstNum + secondNum
}

//var sum = addNums(3, 4);
console.log(addNums (2, 73));

function tip(total, dec){
    return total * dec + total
}

var amount = tip(100, .2);
console.log(amount);


function money(amount, total){


    return amount + total
}
//console.log(money());


//console.log(tip (100, .2));

function tip(firstNum){
    return firstNum
}